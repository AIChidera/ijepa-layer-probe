# ijepa-layer-probe
Layer-wise probe study of I-JEPA vs DINOv2 for unsupervised anomaly detection on MVTec AD and VisA. Shows that final-layer features from masked prediction models collapse geometrically, and that intermediate blocks recover the local structure k-NN scoring depends on.
